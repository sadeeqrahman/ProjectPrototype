<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProjectPrototype" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="backgroundColor">@color/black</item>

        <item name="editTextBackground">@color/edittext_bg_color</item>
        <item name="android:textColor">@color/white</item>
        <item name="myCardbgColor">@color/edittext_bg_color</item>

    </style>

    <style name="Theme.ProjectPrototype" parent="Base.Theme.ProjectPrototype" />
    <style name="RoundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
    </style>

    <style name="Theme.Transition" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>



</resources>